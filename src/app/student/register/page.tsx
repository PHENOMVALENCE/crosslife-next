import RegisterPage from '@/components/auth/RegisterPage';

export const metadata = { title: 'Register - School of Christ Academy' };

export default function StudentRegisterPage() {
  return <RegisterPage />;
}
